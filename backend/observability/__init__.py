"""Observability as one stack: Langfuse client/tracing helpers and the audit
trail all live here as their own modules (langfuse_config.py, tracing.py,
audit.py) - import from the specific submodule you need, e.g.
`from observability.langfuse_config import langfuse`."""
